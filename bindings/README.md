# Bindings

Open wrappers:
- `python-open/`
- `node-open/`
- `go-open/`
- `elixir-open/`

Each wrapper must follow `LICENSE_POLICY.md` and include deterministic dependency lock files for license scanning.
